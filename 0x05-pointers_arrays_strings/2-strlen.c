#include "main.h"
#include <stdio.h>

/**
* _strlen - function that returns the length of a string
* @s: parameter to function
* Return: Always 0.
*/
int _strlen(char *s)
{
	int i, sum;

	for (i = 0; i < s[i]; i++)
	{
		sum = 0 + i;
	}
	return (sum);
}
