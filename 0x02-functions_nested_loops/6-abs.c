#include "main.h"
#include <stdio.h>

/**
* _abs - prints absolute value of a number
* @i: if i takes a negative value
* Return: Always 0 (Success)
*/
int _abs(int i)
{
	if (i < 0)
	{
		i = (-1) * i;
	}
}
