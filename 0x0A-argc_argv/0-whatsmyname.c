#include <stdio.h>

/**
* main - program prints filename
* @argc: counts the number of arguments
* @argv: an array of arguments passed on the command line
* Return: Always 0.
*/
int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
}
